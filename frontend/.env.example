# Copy to .env. Leave as /api to use the Vite dev proxy defined in
# vite.config.ts; point it at an absolute URL when serving the built bundle
# from somewhere other than the API host.
VITE_API_BASE_URL=/api
